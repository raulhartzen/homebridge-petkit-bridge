/** Platform name used in the Homebridge config.json "platform" field. */
export const PLATFORM_NAME = 'PetkitBridge';

/** Plugin name — must match the npm package name. */
export const PLUGIN_NAME = 'homebridge-petkit-bridge';
